{
  "name": "ai4h-evaluation-results",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-submissions.mjs",
    "build": "node scripts/build-results.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test && npm run build"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
